# 1.1.5 讲义：智能交通系统的数据采集、处理和审核流程设计

- **日期**: 2026-08-20
- **状态**: 待讲解 → 待自测 → 待做题（ipynb）
- **数据**: `vehicle_traffic_data.csv`（车辆行驶数据：VehicleID / DriverName / Age / Gender / Speed / TravelDistance / TravelTime / TrafficEvent）
- **考核时间**: 30 分钟，机考

---

## 一、任务解读（数据全流程：采集 → 清洗 → 审核 → 统计）

### 本单元特点：完整数据工程流程

| 1.1.1 | 1.1.2 | **1.1.5（本单元）** |
|---|---|---|
| 读取 + 分组统计 | + 缺失值填充 + 异常标记 | **完整业务流程：采集 → 类型转换 → 范围验证 → 审核 → 保存** |

**核心思维**：真实业务场景的数据处理有**严格流程**，不是拿来就算——必须先清洗、验证合理性，确保数据质量，才能进入分析阶段。

---

## 二、任务拆解（五步流程）

### 步骤①：数据采集

**中文要求**：从 `vehicle_traffic_data.csv` 读取数据并显示前 5 行

**翻译成 pandas**：
```python
data = pd.read_csv('vehicle_traffic_data.csv')
print(data.head())  # 截图保存为 1.1.5-1.jpg
```

**知识点**：[[concepts/pandas-read-csv]]（复用 1.1.1）

---

### 步骤②：数据清洗与预处理（核心）

**中文要求**：
1. 删除缺失值
2. 转换数据类型（Age → int，Speed/TravelDistance/TravelTime → float）
3. 删除异常值（年龄 18-70 / 车速 0-200 / 行驶距离 1-1000 / 行驶时间 1-1440）
4. 保存清洗后的数据

**翻译成 pandas**：
```python
# 1. 删除缺失值
data = data.dropna()

# 2. 数据类型转换
data['Age'] = data['Age'].astype(int)
data['Speed'] = data['Speed'].astype(float)
data['TravelDistance'] = data['TravelDistance'].astype(float)
data['TravelTime'] = data['TravelTime'].astype(float)

# 3. 删除异常值（多条件筛选）
data = data[(data['Age'].between(18, 70)) & 
            (data['Speed'].between(0, 200)) & 
            (data['TravelDistance'].between(1, 1000)) & 
            (data['TravelTime'].between(1, 1440))]

# 4. 保存清洗后的数据
data.to_csv('cleaned_vehicle_traffic_data.csv', index=False)
```

**新知识点**：
- [[concepts/dropna]]：删除缺失值
- [[concepts/astype]]：数据类型转换
- [[concepts/between]]：范围验证（新增，本单元重点）
- [[concepts/to-csv]]：保存 CSV（复用 1.1.2）

---

### 步骤③：数据合理性审核

**中文要求**：标记不合理的数据（不在合理范围内的记录）

**翻译成 pandas**：
```python
# 找出不合理的数据（取反：不在范围内）
unreasonable_data = data[~((data['Age'].between(18, 70)) & 
                           (data['Speed'].between(0, 200)) & 
                           (data['TravelDistance'].between(1, 1000)) & 
                           (data['TravelTime'].between(1, 1440)))]
print(unreasonable_data)  # 截图保存为 1.1.5-2.jpg
```

**知识点**：`~` 取反符号，将「合理数据」条件反转为「不合理数据」

---

### 步骤④：数据统计分析（三项任务）

#### 任务 1：统计每种交通事件的发生次数

```python
traffic_event_counts = data['TrafficEvent'].value_counts()
print(traffic_event_counts)  # 截图保存为 1.1.5-3.jpg
```

**知识点**：[[concepts/value-counts]]：统计分类字段的频次分布（新增）

---

#### 任务 2：统计不同性别的平均车速、行驶距离和行驶时间

```python
gender_stats = data.groupby('Gender').mean()
print(gender_stats)  # 截图保存为 1.1.5-4.jpg
```

**知识点**：[[concepts/groupby-aggregation]]（复用 1.1.1）

---

#### 任务 3：统计不同年龄段的驾驶员数

```python
age_bins = [18, 26, 36, 46, 56, 66, np.inf]
age_labels = ['18-25', '26-35', '36-45', '46-55', '56-65', '65+']
data['AgeGroup'] = pd.cut(data['Age'], bins=age_bins, labels=age_labels, right=False)
age_group_counts = data['AgeGroup'].value_counts()
print(age_group_counts)  # 截图保存为 1.1.5-5.jpg
```

**知识点**：[[concepts/pd-cut-binning]]（复用 1.1.1，但注意 `np.inf` 处理开放区间）

---

## 三、新知识点详解

### ① `.dropna()` —— 删除缺失值

```python
data = data.dropna()
```

**作用**：删除包含 `NaN`（缺失值）的所有行

**对比 1.1.2 的 `.fillna()`**：
- `fillna()`：填充缺失值（保留行，用其他值替代 NaN）
- `dropna()`：删除缺失值（直接删除包含 NaN 的行）

**选择原则**：
- 缺失值少 → `dropna()`（简单粗暴）
- 缺失值多 → `fillna()`（避免丢失太多数据）

---

### ② `.astype()` —— 数据类型转换

```python
data['Age'] = data['Age'].astype(int)
data['Speed'] = data['Speed'].astype(float)
```

**作用**：将列的数据类型转换为指定类型

**常见类型**：
| 类型 | 用途 | 示例 |
|---|---|---|
| `int` | 整数（年龄、数量） | `data['Age'].astype(int)` |
| `float` | 浮点数（速度、距离） | `data['Speed'].astype(float)` |
| `str` | 字符串（ID、名称） | `data['VehicleID'].astype(str)` |

**为什么要转换**：CSV 读取时可能把数字识别为字符串，导致后续计算出错

---

### ③ `.between()` —— 范围验证（本单元核心）

```python
data['Age'].between(18, 70)
```

**作用**：检查每个值是否在指定范围内，返回布尔列

**等价写法**：
```python
# between 一行搞定
data['Age'].between(18, 70)

# 传统写法（两次比较）
(data['Age'] >= 18) & (data['Age'] <= 70)
```

**默认行为**：闭区间 `[18, 70]`（包含边界值）

**开放区间**：`between(18, 70, inclusive='neither')`（不包含边界）

---

### ④ 多条件筛选（复习 + 强化）

```python
# 保留合理数据
data = data[(条件1) & (条件2) & (条件3)]

# 找出不合理数据（取反）
unreasonable = data[~(条件1) & (条件2)]
```

**运算符**：
| 符号 | 含义 | 示例 |
|---|---|---|
| `&` | 与（且） | `(条件1) & (条件2)` |
| `\|` | 或 | `(条件1) \| (条件2)` |
| `~` | 非（取反） | `~(条件)` |

**⚠️ 注意**：每个条件必须加括号，否则运算符优先级会导致错误

---

### ⑤ `.value_counts()` —— 频次统计

```python
data['TrafficEvent'].value_counts()
```

**作用**：统计分类字段每个值的出现次数

**输出示例**：
```
Normal         450
Traffic Jam    230
Accident       180
Breakdown      140
```

**对比 `groupby().count()`**：
- `value_counts()`：单列统计，自动排序
- `groupby().count()`：多列统计，需指定分组列

---

## 四、完整解题流程演示

```python
import pandas as pd
import numpy as np

# 步骤①：数据采集
data = pd.read_csv('vehicle_traffic_data.csv')
print(data.head())  # 截图 1.1.5-1.jpg

# 步骤②：数据清洗
data = data.dropna()
data['Age'] = data['Age'].astype(int)
data['Speed'] = data['Speed'].astype(float)
data['TravelDistance'] = data['TravelDistance'].astype(float)
data['TravelTime'] = data['TravelTime'].astype(float)

data = data[(data['Age'].between(18, 70)) & 
            (data['Speed'].between(0, 200)) & 
            (data['TravelDistance'].between(1, 1000)) & 
            (data['TravelTime'].between(1, 1440))]

data.to_csv('cleaned_vehicle_traffic_data.csv', index=False)

# 步骤③：数据审核
unreasonable_data = data[~((data['Age'].between(18, 70)) & 
                           (data['Speed'].between(0, 200)) & 
                           (data['TravelDistance'].between(1, 1000)) & 
                           (data['TravelTime'].between(1, 1440)))]
print(unreasonable_data)  # 截图 1.1.5-2.jpg

# 步骤④：数据统计
# 任务1：交通事件频次
traffic_event_counts = data['TrafficEvent'].value_counts()
print(traffic_event_counts)  # 截图 1.1.5-3.jpg

# 任务2：不同性别的平均值
gender_stats = data.groupby('Gender').mean()
print(gender_stats)  # 截图 1.1.5-4.jpg

# 任务3：不同年龄段的驾驶员数
age_bins = [18, 26, 36, 46, 56, 66, np.inf]
age_labels = ['18-25', '26-35', '36-45', '46-55', '56-65', '65+']
data['AgeGroup'] = pd.cut(data['Age'], bins=age_bins, labels=age_labels, right=False)
age_group_counts = data['AgeGroup'].value_counts()
print(age_group_counts)  # 截图 1.1.5-5.jpg
```

---

## 五、易错点（考试陷阱）

### ⚠️ 陷阱 1：`.between()` 里忘记加括号

```python
# ❌ 错误（运算符优先级导致逻辑错误）
data = data[data['Age'].between(18, 70) & data['Speed'].between(0, 200)]

# ✅ 正确
data = data[(data['Age'].between(18, 70)) & (data['Speed'].between(0, 200))]
```

---

### ⚠️ 陷阱 2：审核任务里忘记取反符号 `~`

```python
# ❌ 错误（找出的是合理数据，不是不合理数据）
unreasonable = data[(data['Age'].between(18, 70))]

# ✅ 正确
unreasonable = data[~((data['Age'].between(18, 70)))]
```

**记忆技巧**：审核任务要找「不合理」的数据 = 「不」满足条件 = `~`（取反）

---

### ⚠️ 陷阱 3：年龄分箱的开放区间处理

```python
# 题目要求：65岁以上
# ❌ 错误（bins 最后一个值写固定数字，会漏掉超出的数据）
age_bins = [18, 26, 36, 46, 56, 66, 100]

# ✅ 正确（用 np.inf 表示无穷大，包含所有大于 66 的值）
age_bins = [18, 26, 36, 46, 56, 66, np.inf]
```

---

### ⚠️ 陷阱 4：`.astype()` 转换前未清洗缺失值

```python
# ❌ 错误（NaN 无法转换为 int，会报错）
data['Age'] = data['Age'].astype(int)
data = data.dropna()

# ✅ 正确（先删除 NaN，再转换类型）
data = data.dropna()
data['Age'] = data['Age'].astype(int)
```

**原则**：数据清洗顺序：**先删除/填充缺失值 → 再转换类型**

---

## 六、自测题

1. `.between(18, 70)` 和 `(df['Age'] >= 18) & (df['Age'] <= 70)` 的结果相同吗？哪个更简洁？
2. 为什么步骤②里要先 `dropna()` 再 `.astype(int)`？顺序反了会怎样?
3. 审核任务里的 `~` 符号是什么意思？为什么要用它？
4. `pd.cut` 的 `bins=[18, 26, 36, ..., np.inf]` 里的 `np.inf` 有什么作用？能用 `1000` 代替吗？

<details>
<summary>参考答案（先自测再看）</summary>

1. **结果相同，`.between()` 更简洁**：
   - 传统写法需要两次比较 + 一次 `&`
   - `.between()` 一个方法搞定，代码更易读

2. **先删缺失值，再转类型**：
   - `NaN`（缺失值）无法转换为 `int` 类型，会报错 `ValueError`
   - 必须先 `dropna()` 删除 NaN，再 `astype(int)` 转换

3. **`~` 是取反符号（逻辑非）**：
   - 审核任务要找「不合理」的数据
   - 原条件是「合理范围」，加 `~` 反转为「不在合理范围内」

4. **`np.inf` 表示正无穷大**：
   - 处理开放区间「65岁以上」（无上限）
   - 不能用 `1000` 代替：如果有 101 岁的数据，会被排除在外（导致数据丢失）

</details>

---

## 七、关联概念（已入知识库）

- [[concepts/pandas-read-csv]]（1.1.1 复用）
- [[concepts/dropna]]（新增）
- [[concepts/astype]]（新增）
- [[concepts/between]]（新增，本单元核心）
- [[concepts/value-counts]]（新增）
- [[concepts/groupby-aggregation]]（1.1.1 复用）
- [[concepts/pd-cut-binning]]（1.1.1 复用）
- [[concepts/to-csv]]（1.1.2 复用）
